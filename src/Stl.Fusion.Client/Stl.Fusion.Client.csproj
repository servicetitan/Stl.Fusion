<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <OutputType>Library</OutputType>
    <PackageTags>servicetitan stl stl_fusion client</PackageTags>
    <Description>Client-side components of Stl.Fusion - a Knockout.js-style Knockout.js-style computed/observable <!-- 
      -->abstraction designed to power distributed apps: thread-safe, asynchronous, immutable, and ready to <!--
      -->provide its own replicas (~ proxies that are connected to the original computed/observable instance) <!--
      -->for remote clients.<!--
    --></Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Stl.Fusion\Stl.Fusion.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="RestEase" />
  </ItemGroup>

</Project>
