@if (Exception != null) {
    <Error @key="@Exception" Title="Action failed!" Dismissed="@Dismissed">@Exception.Message</Error>
}

@code {
    [Parameter]
    public Exception? Exception { get; set; }
    [Parameter]
    public EventCallback Dismissed { get; set; }
}
